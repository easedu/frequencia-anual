/**
 * Barrel Export: React Query Hooks (Alias)
 *
 * Re-exports tudo de index-query.ts para permitir imports mais curtos:
 * import { useStudents } from '@/hooks/api/query';
 *
 * É equivalente a:
 * import { useStudents } from '@/hooks/api/index-query';
 */

export * from './index-query';
